dist: trusty
language: python
python:
  - "3.5"
  - "3.6"
env:
  - AIOHTTP="2"
  - AIOHTTP="1"
install:
  - pip install -U setuptools
  - pip install -U pip
  - pip install -r requirements.txt
  - pip install -r test/requirements.txt
  - pip install -e .
script:
  - py.test
